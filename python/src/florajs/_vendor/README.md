# Vendored Flora bundle

`flora.iife.js` is built from the JS library at the repository root and copied
here by `npm run build:python`. It is gitignored; wheels and sdists include it
via the `artifacts` setting in `pyproject.toml`.
